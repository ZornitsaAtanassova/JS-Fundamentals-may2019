function chartsToString(first, second, third) {
    //Write a function which receives 3 parameters. Each parameter is a single character. Combine all the characters into one string and print it on the console.
    
    //1)Concatenation or Interpolation
    //2)Print

    let charToString = first + second + third;
    console.log(charToString);
}

chartsToString('a', 'b', 'c');     //abc
chartsToString('%', '2', 'o');     //%2o
chartsToString('1', '5', 'p');     //15p
