function reversedOrderChars(first, second, third) {
    //Write a program that takes 3 parameters (characters) and prints them in reversed order with a space between them.

    //1)Just print in wanted order with interpolation for spaces
    
    console.log(`${third} ${second} ${first}`);
}

reversedOrderChars('A', 'B', 'C');   //C B A
reversedOrderChars('1', 'L', '&');  //& L 1
